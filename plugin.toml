schema_version = 1
name = "mysql"
version = "0.1.0-rc.1"
description = "bounded MySQL 8.4 protocol driver for Sigil"
license = "MIT"

[component]
file = "plugin.wasm"
entrypoint = "sigil:sql/driver@0.1.0"

[requires]
sigil = ">=0.31.0, <1.0.0"
host_api = "^1.0"

[capabilities]
network = true
secrets = true

[repository]
source = "github:sigil-plugins/mysql"
